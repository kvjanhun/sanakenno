node_modules/
dist/
.output/
.env
.env.local
*.log
*.db*
server/data/kotus_words.txt
.DS_Store
.vite/
.expo/
coverage/
reports/
test-results/
playwright-report/
.vscode/
package-lock.json
.turbo/
*.tsbuildinfo
tmp/
# Agent config. Skills are tracked; session state and machine-local settings are
# not. Shared skill bodies live in the nuc repo — the symlinks here resolve to
# ../../../nuc/skills, so the three repos must stay siblings under ~/Projects.
.claude/*
!.claude/skills/
.agent

